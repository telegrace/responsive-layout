import { Box } from "@chakra-ui/react";

const SidebarPage: React.FC<any> = (props) => {
  return (
    <Box bg="tomato" w={[null, null, 500]} p={4} color="white">
      This is the Box
    </Box>
  );
};

export default SidebarPage;
